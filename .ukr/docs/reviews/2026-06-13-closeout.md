*🇺🇦 Це український переклад. Оригінал англійською: `docs/reviews/2026-06-13-closeout.md`.*

# Закриття огляду — Semantic Code Search

**Дата:** 2026-06-13
**Вхідні дані:** `2026-06-11-architecture-review.md` (зауваження) і
`2026-06-11-production-readiness-plan.md` (4-фазний план роботи).
**Мета:** підтвердити, що кожне зауваження огляду закрите, із задачею, що його закрила,
і доказом. Одне зауваження **частково** закрите — див. M7.

Усі чотири фази злиті в `main` (PR #1–#18). Критерій "готово коли" Фази 2 був
виконаний оцінюванням (Recall@5 0.600 → 0.912, nDCG@5 0.343 → 0.760); Фази 3 —
системою лише на env, з можливістю розгортання в контейнері, покритою тестами.

---

## Зауваження → вирішення

### Критичні
| ID | Зауваження | Закрито | Як |
|----|---------|-----------|-----|
| C1 | `mcp_server.py` не є справжнім MCP-сервером | task 1.1 | Переписано з офіційним `mcp` SDK (FastMCP/stdio); правильний `.mcp.json`. Протокол покритий `tests/test_mcp_protocol.py`. |
| C2 | Перший запит падає + ламає кеш BM25 | task 0.2 | Порожній список вузлів тепер кидає чітку помилку "run build_index.py"; BM25 перебудовується з Qdrant (без pickle). Регресійний тест у `tests/test_query_index.py`. |
| C3 | Кожен запит платить за повний холодний старт | task 1.2 | Довготривалий сервіс Flask тримає прогрітий рушій; другий запит значно менше 1 с. |
| C4 | Крихкий запуск процесу (`python` у PATH) | task 0.3 → 1.1 | Модель підпроцесу повністю замінено (MCP-прошарок → HTTP до сервісу). |

### Високі
| ID | Зауваження | Закрито | Як |
|----|---------|-----------|-----|
| H1 | Математика об'єднання оцінок неправильна (min-max на змішаних шкалах) | task 2.1 | Reciprocal Rank Fusion (`ranking.rrf_scores`); об'єднує лише за рангом. Оцінювання покращилося. |
| H2 | Кандидати відсікаються перед ранжуванням | tasks 2.1–2.2 | Відсікання тепер відбувається **після** об'єднання (`RERANK_CANDIDATES`); просвіп показав відсутність втрати recall. |
| H3 | Кеш ембедингів: необмежений, повільні записи, без перевірки моделі | task 3.2 | SQLite-кеш з ключем `(model_name, text_hash)`, один запис/запит, витіснення LRU. |
| H4 | Немає закріплення залежностей | task 0.1 | Закріплено `requirements.txt` + `requirements-dev.txt`; CI їх встановлює. |
| H5 | Конвеєр має нуль тестів | tasks 0.5, 3.6 | CI (ruff + pytest) з 0.5; 3.6 додав тести конвеєра/MCP-протоколу/реального Qdrant. 112 тестів. |
| H6 | Перебудова видаляє спочатку, будує потім | task 3.3 | Будує в `<name>-<timestamp>`, обмінює аліас Qdrant, потім видаляє старий — безпечно при збоях. |
| H7 | Захардкоджена конфігурація; демо ділить прод-колекцію | tasks 3.1, 2.3 | `config.py` (pydantic-settings) шар env; варіанти запиту конфігуровані; демо використовує свою власну колекцію `demo-eval`. |

### Середні
| ID | Зауваження | Закрито | Як |
|----|---------|-----------|-----|
| M1 | Важка робота під час імпорту | task 3.7 | `model_setup.setup_models()` лінива + захищена; імпорт не завантажує модель/torch. Фейк у conftest прибрано. |
| M2 | Pickle як формат зберігання (ризик RCE) | Phase 1 + 3.2 (+ guard 3.8) | Жодного pickle ніде (BM25 з Qdrant, SQLite-кеш). `tests/test_no_pickle.py` це забезпечує. |
| M3 | Слабка перевірка якості відповіді + мізерний бюджет контексту в `ask.py` | task 3.9 | Повтор на основі співпадіння фраз прибрано (єдиний виклик LLM); бюджет `MAX_CONTEXT_TOKENS`, рахований у токенах. |
| M4 | `ask.py` змішує логи в stdout | tasks 3.5, 3.9 | Прапорець `DEBUG` зник; `logger.debug` → stderr; лише відповідь у stdout. |
| M5 | Інтерактивний `input()` блокує автоматизацію | task 0.4 | Прапорець `--force`/`-y`; `run_demo.sh` більше не потребує `echo y`. |
| M6 | Немає інкрементного індексування | task 3.4 | `--incremental`: хеш вмісту на файл у payload; переембед змінених/нових, видалення вилучених. |
| M7 | `_retrieve_cache` росте без обмеження **і** марний на запит | tasks 1.2 + закриття | Половину "марний" виправлено прогрітим сервісом (1.2). Половину "росте без обмеження" закрито в цьому закритті: `_retrieve_cache` тепер обмежений LRU до `RETRIEVE_CACHE_SIZE` (за замовчуванням 256) — pop+reinsert при попаданні, видалення найдавніше використаного при переповненні (`query_index.hybrid_retrieve`). |
| M8 | `SentenceSplitter` на коді C# | task 2.4 | Розбиття на фрагменти з урахуванням коду через tree-sitter; найбільший виграш оцінювання (Recall@5 0.787 → 0.912). |
| M9 | Поламані посилання в документації (TODO.md) | task 0.6 | Мертві посилання виправлено. |
| M10 | Qdrant без аутентифікації; `ensure_qdrant` використовує Docker усередині контейнера | tasks 3.8, 1.2/3.1 | `QDRANT_API_KEY` + `QDRANT_HTTPS` течуть до клієнта; `QDRANT_AUTOSTART=0` чекає замість виклику shell. |
| M11 | Полірування: неанглійський коментар, друкарські помилки, логування через `print` | tasks 3.5 (+ попередні рефакторинги) | Модуль `logging` замінює затінений `print`; коментар/друкарські помилки виправлені в попередніх рефакторингах. |

**Підсумок:** **19 з 19 зауважень повністю закриті.** Залишок M7 (необмежений
`_retrieve_cache`) був знайдений під час цього закриття і виправлений у тому ж проході —
кеш тепер обмежений LRU до `RETRIEVE_CACHE_SIZE` (за замовчуванням 256), з модульним
тестом (`tests/test_pipeline_query.py::test_retrieve_cache_evicts_least_recently_used`).

---

## Перевірка (запущено на `main`, 2026-06-13)

- `pytest tests/ -q` → **114 passed** (повний стек; важкі тести пропускаються без нього).
- `ruff check scripts/ eval/ tests/` → чисто.
- `tests/test_no_pickle.py` → проходить (без `pickle`/`torch.load`/`joblib`/`dill`).
- `bash eval/run_demo.sh` → **1.000 / 1.000 / 1.000** (Recall@5 / MRR / nDCG@5).
- Наскрізний dogfood проти реального індексу `lfm`:
  `query_index.py "where is the user password verified at login"` повернув 8
  ранжованих результатів (`ActivateAccount.cshtml.cs`, `LoginFormModel.cs`,
  `LoginUserCommand.cs`, …) як чистий JSON у stdout, логи в stderr.
- Доступні ручні CI-задачі для важкого шляху: `eval.yml` (шлюз оцінювання реального Qdrant)
  і `tests-full.yml` (повний набір вкл. інтеграційний тест testcontainers).

---

## Поза обсягом (без змін з плану)

Не зроблено, навмисно: інші мови окрім .NET (чанкер тримається
придатним до підключення модулів), автоматичне оновлення індексу усередині сервісу (спостереження за файлами — тепер
розблоковане інкрементним індексуванням 3.4, природна наступна функція), і масштабування
понад один екземпляр сервісу.
